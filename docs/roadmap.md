# Roadmap

> Living document: the catalogue of phases. The hand-off to `/plan`, which the
> human points at a phase; `/plan` then drafts its spec + issues and links them
> back here. No status markers and no current-focus pointer — the human chooses
> what to plan next; progress lives in the GitHub milestone each phase links to.

## Phase overview

| Phase | Name | Spec | Milestone |
|---|---|---|---|
| P1 | Drop spec lifecycle state (DRAFT/READY) | [spec-P1](specs/archive/spec-P1.md) | [#1](https://github.com/skrischer/loopkit/milestone/1) |
| P2 | Drop roadmap current-focus + status; manual targeting | [spec-P2](specs/archive/spec-P2.md) | [#2](https://github.com/skrischer/loopkit/milestone/2) |
| P3 | Proportional ceremony: living-spec milestone + `track:adhoc` fast-lane | [spec-P3](specs/archive/spec-P3.md) | [#3](https://github.com/skrischer/loopkit/milestone/3) |
| P4 | Prior-art elevation: mandatory consult + linking; template challenge fields | [spec-P4](specs/archive/spec-P4.md) | [#4](https://github.com/skrischer/loopkit/milestone/4) |
| P5 | `/implement` as milestone orchestrator; `/plan` emits milestone-level depends-on | [spec-P5](specs/archive/spec-P5.md) | [#5](https://github.com/skrischer/loopkit/milestone/5) |
| P6 | Clarification belongs to planning: planner anticipates implementer questions; implementer escalates forks back | [spec-P6](specs/archive/spec-P6.md) | [#6](https://github.com/skrischer/loopkit/milestone/6) |

The milestone (open/closed + issue progress) is where status lives.

## Dependencies & parallelization

Applying the milestone-level `depends-on` principle to loopkit itself — with an
honest correction surfaced during planning: loopkit's phases are **not**
independent. P2–P6 all edit `skills/plan/SKILL.md`, and P1/P3/P5/P6 also edit
`skills/implement/SKILL.md`. Because they share these core files, the phases run
as a **sequential** chain, not parallel orchestrators:

- Run order: **P1 → P2 → P3 → P4 → P5 → P6**. Each milestone carries a
  `Depends on milestone: #<n>` line to its predecessor.
- The earlier "P1/P2/P4 are parallelizable" assumption was optimistic; the
  file-level reality serializes them. That is itself a dogfood finding: on a
  small, shared-file codebase the parallelizable frontier is narrow — the
  orchestrator's parallelism pays off later, on feature work that touches
  disjoint files.
- Within a milestone, issues touching different files run in parallel; issues
  touching the same file are serialized by their `Depends on:` lines.
- The inception challenge lens (part of P4) is already bootstrapped in
  `skills/inception/SKILL.md`.

## Features (post-redesign)

The P1–P6 redesign is complete; new work is planned directly via
`/loopkit:plan <scope>` and lands here as a catalogue row.

| Feature | Spec | Milestone |
|---|---|---|
| tooling-preflight — fail fast when git/gh are missing or unauthorized | [spec](specs/archive/spec-tooling-preflight.md) | [#7](https://github.com/skrischer/loopkit/milestone/7) |
| inception-prior-art-coupling — bidirectional prior-art↔roadmap coupling: every plannable phase backed by prior art (Step 2 ↔ Step 6) | [spec](specs/archive/spec-inception-prior-art-coupling.md) | [#8](https://github.com/skrischer/loopkit/milestone/8) |
| design-phase — optional, tool-agnostic design step anchored to the spec (project docs/design.md contract); reviewed at the spec-acceptance gate | [spec](specs/archive/spec-design-phase.md) | [#9](https://github.com/skrischer/loopkit/milestone/9) |
| roadmap-iteration — extract ongoing feature/phase planning into `/loopkit:roadmap`: prior-art-driven idea sparring that seeds one or many phases per loop, may revise foundation docs (vision/constitution/architecture — ratified at spec-acceptance, never in implement), no readiness sweep, delegates to inception (DRY); `/loopkit:plan` gains the matching multi-milestone loop | [spec](specs/archive/spec-roadmap-iteration.md) | [#10](https://github.com/skrischer/loopkit/milestone/10) |
| prior-art-elevation — raise prior-art research to loopkit's core substrate (more weight in vision/constitution) + carry `/plan`'s OSS source-checkout into the implement loop as a template | [spec](specs/archive/spec-prior-art-elevation.md) | [#11](https://github.com/skrischer/loopkit/milestone/11) |
| design-in-the-loop — broaden design from UI-only to "a visualisation clarifies the decision" (UI + concept/flow/architecture), woven into the roadmap+plan sparring; durable medium = committed SVG (renders in GitHub's file/rich view, not the raw PR diff), Mermaid the GitHub-native alternative; extends inception Step 7b + the constitution design principle. Constitution stays medium-agnostic; loopkit's own docs/design.md picks committed SVG | [spec](specs/archive/spec-design-in-the-loop.md) | [#12](https://github.com/skrischer/loopkit/milestone/12) |
| structural-verify — set loopkit's `Verify` (currently `none`) to the native `claude plugin validate` (JSON validity + skill/agent/command frontmatter + duplicate names + path traversal — no new deps); wire it into the per-PR + milestone-QA gates; add a thin shell/git check only for loopkit invariants the native validator misses | [spec](specs/archive/spec-structural-verify.md) | [#13](https://github.com/skrischer/loopkit/milestone/13) |
| implement-frontier-exits — close the orchestrator wave loop's undefined exit states: an issue left open without a label by a subagent that reports none of merged/escalated/parked re-enters the frontier and re-dispatches forever — treat it as a failed dispatch (one retry, then auto-park `blocked:human`); report cross-milestone blocks alongside escalated/parked; wire the workflow contract's bounded-retry rule (same failure twice -> stop) into implement's review loop and plan's step-6 review by reference, never by copy | [spec](specs/archive/spec-implement-frontier-exits.md) | [#15](https://github.com/skrischer/loopkit/milestone/15) |
| implement-resume-safety — resume idempotency (check for an existing branch/open PR via `gh pr list --head` before `git worktree add -b`; re-attach instead of colliding), made a **per-dispatch invariant** (not just resume-time) so it also catches post-compaction re-dispatch; **wave-boundary compaction** — name the wave boundary (step 3 "await the whole batch" is a hard barrier with no live subagents; all state re-derivable from GitHub after step 4) as the **only safe native `/compact` point** (never mid-wave), reuse the native compactor via a CLAUDE.md "Compact Instructions" line preserving milestone target + frontier, optionally `/compact` at a boundary when context runs high — reimplement nothing; plus `--limit` on every gh list call and an inception-recorded ProjectV2 field-ID recipe. Split 2026-07-08: the merge-conflict/file-overlap half moved to `implement-conflict-recovery` | [spec](specs/archive/spec-implement-resume-safety.md) | [#16](https://github.com/skrischer/loopkit/milestone/16) |
| implement-conflict-recovery — a defined merge-conflict path (rebase onto the updated base, re-run Verify, re-merge once; a second conflict routes to `needs:planning` — a missed serialization edge is a planning defect) + a soft file-overlap pass in plan (predict touched files per issue, add `Depends on:` edges or overlap notes) replacing implement's unestablished same-file guarantee. Split out of `implement-resume-safety` on 2026-07-08: bites only on a genuine parallel frontier over overlapping files — insurance, not a live pain on serial/disjoint milestones. Backed by the same `implement-resume-safety` prior-art concern (CoAgent merge-selection gap; CCPM `conflicts_with`) | — | — |
| ceremony-overhead — cut the full-spec track's fixed process-PR overhead (~3 per milestone -> 1: fold the roadmap-link commit onto the accepted spec branch, keep the QA archive PR as the single close-out) and fix the unexecutable post-merge Decision-log instruction (write decisions pre-merge on the issue branch; QA close-out as catch-all). Own telemetry as of 2026-07-02: 44 of 86 merged PRs carried no behavior change. Foundation impact: constitution — one-line adjustment to the "accepted = merged with a milestone and issues" wording (milestone creation briefly precedes the spec merge). **[2026-07-08 audit re-scope]** add the model-tier dimension: a no-behavior (docs/roadmap-link) PR must not burn a top-tier review subagent — ~half of review spend went to the 44 no-op PRs; route no-source-change diffs to a cheaper reviewer, compounding with `model-tier-slots` | [spec](specs/archive/spec-ceremony-overhead.md) | [#17](https://github.com/skrischer/loopkit/milestone/17) |
| gate-evidence — make milestone-QA evidence durable on GitHub: post the derived QA scenarios + the human verdict as a milestone comment before closing (the living-spec batch summary gets the same durable home) + a QA handover mapping each spec Verification item to the PRs/commits implementing it, so a gate stop costs minutes, not archaeology. **[2026-07-08 audit re-scope]** telemetry shows the gate is today rubber-stamped, not skipped-then-caught: all 13 milestones closed 3–13s after their archive PR merged, and 2 of the 3 fix commits in all history rode instant-closed gates (M5 worktree leak; SKILL.md YAML broken since bootstrap, ~15 days across 12 gates). So enforce **ordering** — the human verdict (which MAY be an explicit recorded `waived`) is posted as the milestone comment **before the archive PR merges**, else the instant-close pattern posts evidence nobody read. Keep exactly two hard gates; this makes today's de-facto skipping auditable without a third gate or an autonomy dial | — | — |
| design-coverage — commit and act on the Flowmate feedback (docs/feedback/2026-06-19-design-skill-retrofit.md): /design derives the spec's user-facing capabilities and asserts each maps to a surface or state, writes back a complete `Design:` line per spec, splits its review into craft + coverage passes (both under bounded retry); a missing docs/design.md routes to inception --here instead of growing a second bootstrap; /roadmap recognizes docs/feedback/ files as first-class raw-idea inputs and surfaces unseeded ones. **[2026-07-08 audit re-scope]** design is read at implement Orient but **dropped from the merge review gate** (the review subagent is reseeded with only diff + constitution/CLAUDE.md) — reseed the committed design artifact into implement's review gate for UI-surface issues so merge checks surface coverage (evidence: the #12 SVG broke the no-status-marker rule, unseen at both the spec-acceptance gate and merge, fixed post-close in #132). **Anti-creep guard:** bound coverage enforcement to full-spec milestones; exempt `track:adhoc` and living-spec batches explicitly — sparring is a dialog, not a gate | — | — |
| permission-template-hardening — replace the hardcoded Supabase deny rules in the inception settings template with the placeholder convention (verify.sh asserts the placeholder is intact or replaced, never the shipped example); close cheap deny-list gaps (`rm -r`/`-fr` spellings, `+refspec` force-push, `git -C` variants of guarded commands); document why bypassPermissions is the attended-loop default and offer an acceptEdits variant as an explicit inception choice. **[2026-07-08 audit re-scope]** reconcile the contradiction that `.claude/settings.json` sets `bypassPermissions` **globally/unscoped** while CLAUDE.md claims autonomy is "scoped to loopkit skills" a permission *mode* is session-state, not skill-state, so the claim is unenforceable as written). **[2026-07-08 decision] build real scoping, don't just document it:** set `defaultMode` to a restrictive value (e.g. `default`/`acceptEdits`) and add a `PreToolUse` hook in the frontmatter of `plan`/`implement`/`inception` that grants exactly the needed autonomous actions (commit/push/`gh` merge/dependency install) `permissionDecision: allow` — natively scoped to the skill lifecycle and auto-cleaned when the skill ends (settings.json `deny` rules still always win). Open sub-question for the spec: whether frontmatter-hook cleanup applies cleanly per wave under parallel `/implement` fan-out. Document bypass as the amplifier leg; add the `--force-with-lease` deny gap. **Must NOT be sold as trust-boundary closure** — a deny-list guards shell-exec only and cannot touch the instruction-injection path; subordinate to / paired-after `trust-boundary`. (The no-headless/API-key/cron rule is already enforced by `scripts/verify.sh`'s config-surface guard, not settings.json) | — | — |
| repositioning — rewrite README.md + plugin.json around the proportional dial and the winning vocabulary (attended, spec-anchored loop engineering under the agentic-engineering umbrella): three-track table, one walkthrough per lane backed by dogfood data, orchestrator fan-out GIF, remote install, uninstall/footprint section, Codex/multi-harness FAQ as a documented stance; gates justified via review bottleneck/comprehension debt/security, never model unreliability; extend scripts/verify.sh with a README-only drift assertion (fail on "READY spec"/"select/claim"). Foundation impact: vision — add the positive "attended orchestration between exactly two human gates" claim; the constitution's grep-verifiable no-scheduler rule stays untouched. **[2026-07-08 audit re-scope]** lead the README on the surviving unique wedge — a **dependency-ordered frontier computed from GitHub issue relations, fanned out by one orchestrator** (no practitioner in the research window ships this); frame no-scheduler as a deliberate stance, not the headline differentiator (Anthropic's Jun-30 four-rung loop ladder normalized scheduled/unattended loops, so "attended" alone no longer differentiates). Cite Ronacher's "harness loop" vocabulary as adopt-validation. The README drift-assertion is confirmed load-bearing: the shipped README today still says "READY spec" and "/implement … select/claim" — live constitution violations. **Prerequisite:** resolve the `/batch`-vs-Dynamic-Workflows open question before locking vocabulary (reuse-native-primitives self-test) | — | — |
| prior-art-backfill — backfill the competitive landscape (GitHub Copilot coding agent + Agent HQ into the single-entry USP section; OpenAI Codex/AGENTS.md, BMAD, OpenSpec, GSD, one parallel-orchestration UI) with per-concern adopt/avoid verdicts + a lightweight freshness contract: `Verified: <date>` distinct from creation date, a volatility flag on product-feature claims, a staleness check in inception --here's readiness sweep + index repair (tag the untagged legacy sections, fix stale "Point 3" references, update the passes header). **[2026-07-08 audit re-prioritise]** re-verify the **load-bearing USP verdicts FIRST** (the spec-kit AVOID is flagged stale since GH-Issues integration reopened; the USP is defended by only one entry, CCPM) — `/plan` links these verdicts as the spec decision substrate, so a stale verdict propagates a wrong adopt/avoid into live planning. Add the **software-factory / Dark-Factory avoid-anchor** entry (Shapiro Level-5, StrongDM no-review, Factory.ai owns the term commercially; Osmani uses "Factory Model" — counter-position, never adopt as loopkit's brand) and the Ronacher harness-loop entry. Add a **read-by-index-not-whole-file assertion + per-doc size budget** (prior-art.md is ~13.2k tokens, ~3.8× the next doc, and this row grows it). Make the `(feature: slug)` coupling **grep-verifiable** and backfill the 8 untagged legacy sections. **Must precede `repositioning` + `launch-playbook`.** | — | — |
| dogfood-untested-claims — exercise the two untested headline claims: open a human-initiated living-spec milestone for the ongoing correction/feedback-intake stream (exercises the never-used QA batch-summary path) and, when two spec-worthy phases with verified-disjoint file sets exist, run two /loopkit:implement orchestrators concurrently; record both outcomes here like the P1–P6 serialization finding; never inflate a trivial change into a spec to force the test. **[2026-07-08 audit re-scope]** the two runs only exercise path-existence — add an instrumentation beat that records **one concrete instance where an attended gate/review caught a defect an unattended loop would have merged**, the evidentiary core of the launch narrative ("attended caught what unattended would miss", backed by Osmani/Gas Town). Record such instances when they naturally occur; the no-inflation bar stays (no manufacturing) | — | — |
| launch-playbook — run the proven methodology-plugin launch sequence while "loop engineering" is hot: first-person dogfood narrative with GitHub state as proof (all cited figures re-verified pre-publication), HN + Simon Willison pitch, submission to the official Anthropic directory and awesome-claude-code, visible release cadence. Human prerequisites: LICENSE + repo metadata (fast-lane) landed; depends on repositioning (the README must match the constitution before strangers read both). **[2026-07-08 audit re-scope]** add a **subscription-auth cost-control beat** preempting the Jul-5 "Loop Engineering Is Dead" backlash — which attacks the practice on *cost* (Uber exhausting its AI budget in four months; Willison's own loop ~$149; StrongDM ~$1000/day) — subscription-auth-only + the proportional dial is the precise rebuttal (pair with `model-tier-slots` as the throughput half). Timing is now: Anthropic canonized the term Jul 7 mid-backlash, window is weeks not months. Add a **loop/software-factory name-disambiguation check** (e.g. looplabs.fun "first autonomous software factory") before directory submission; verify **remote installability** (marketplace source is `./` today — local-path only) as a hard precondition | — | — |
| foundation-dedup — dedup the permanently-loaded flagship docs (vision Scope bullets become one-line pointers to constitution principles; the constitution's design principle shrinks to its binding core with mechanism detail moving to docs/design.md), single-source the drifting hard-limit enumerations to .claude/settings.json, **remove** docs/design.md's German-diagram exception (**[2026-07-08 decision]** enforce English-only per the binding convention; the separate SVG-vs-Mermaid *medium* question is deferred, not bundled into this row). Foundation impact: vision + constitution — the dedup is the change itself, authored in the plan cycle and ratified at spec-acceptance. **[2026-07-08 audit re-scope]** while touching the permanently-loaded surface, **slim the `roadmap` (310 tok) and `plan` (250 tok) SKILL.md frontmatter descriptions** to a one-line trigger+scope matching the ~170-token peers — they are ~1.5–1.8× their peers and together half the always-loaded tax every target project pays each session (highest-leverage always-loaded slim available). The German-exception ratify-or-remove is confirmed needed: the one real committed design artifact is authored in German, unreviewable by non-German reviewers and degrading LLM-as-reviewer at the spec gate, an exception the binding constitution never sanctioned | [spec](specs/spec-foundation-dedup.md) | [#19](https://github.com/skrischer/loopkit/milestone/19) |
| **trust-boundary** *(NEW — 2026-07-08 audit)* — a constitution trust-boundary principle plus its skill-level enforcement, resolving the 2026-07-08 state-machine note: **read-discipline** (issue/PR/comment/bot text is inert data — no attachment fetch, no in-body URL follow, no payload exec), **shell-hygiene** (never interpolate GitHub-sourced strings unquoted into a shell — fixes the live defect at `skills/plan/SKILL.md:226-232` where `<scope>`/`<step>`/spec text is interpolated unescaped into `gh api -f description=` / `gh issue create --body=`, which breaks or executes on backticks/`$()`/quotes on the human-authored write path today), and **Rule-of-Two** (a human gate when untrusted-input + sensitive-access + external-comms coincide). Reframe the `track:adhoc` body as an **inert request to plan against, never an executable contract**, and require trusted authorship (`author_association` OWNER/MEMBER/COLLABORATOR) or explicit human selection before loop-mode auto-picks an adhoc issue (today: label + board-Todo + deps-closed drives an externally-authored body to autonomous squash-merge with no QA gate and no author check). Foundation impact: constitution — new binding principle, authored in the plan cycle, ratified at spec-acceptance. **Hard predecessor of any autonomy-dial change; paired-before `permission-template-hardening` (a deny-list is necessary-not-sufficient).** | [spec](specs/archive/spec-trust-boundary.md) | [#14](https://github.com/skrischer/loopkit/milestone/14) |
| **model-tier-slots** *(NEW — 2026-07-08 audit)* — an OPTIONAL `docs/workflow.md` field mapping role→tier (orchestrator / implementer / reviewer), default `inherit` when absent, read by `/implement` and `/plan` and passed to native subagent model selection; model names live **only** in the per-project contract (where the Verify command already lives), never in tool-agnostic skill prose (same class as the "no external-tool URL as durable state" rule). Reuses native mechanism (subagent frontmatter `model` / resolution order / `opusplan`) — nothing to build. Must warn that `CLAUDE_CODE_SUBAGENT_MODEL` globally overrides the routing (documented foot-gun). Codifies the split the maintainer hand-prompts every run (top-tier orchestrates, cheaper worker implements) and answers the throughput half of the cost backlash; compounds with `ceremony-overhead`'s no-op-PR reviewer-tier point | [spec](specs/archive/spec-model-tier-slots.md) | [#18](https://github.com/skrischer/loopkit/milestone/18) |
| **prior-art-threading** *(NEW — 2026-07-08 audit)* — make prior art the *derive-from default* the constitution already claims it is, at the two flow steps where it is stated-but-not-wired: (1) `/implement` Orient reads the spec's Prior-art verdicts **by default** (today reachable only via the opt-in OSS checkout "MAY, when stuck on technique", excluded for `track:adhoc` — the shipped `prior-art-elevation` threaded the checkout *mechanism*, not the read *default*; keep the clone opt-in, add a mandatory orient-time read of the verdicts, not a clone); (2) wire inception **Step 4** (constitution derivation) to name the adopt/avoid prior-art each tech-stack/principle row derives from, mirroring Step 3 (vision), which is the only step wired today. Backed by the constitution prior-art-substrate principle + `loopkit source-checkout precedent (PR #93)` + `loopkit dogfooding finding (Rack)` | — | — |
| **ship-skill** *(NEW — 2026-07-08)* — a sixth loop skill `/loopkit:ship` that finalizes a milestone's merged work into a **published release** — the attended, GitHub-native release phase most SDD tools omit (of the surveyed frameworks only BMAD models the lifecycle through deployment; spec-kit/Kiro/OpenSpec stop at implement). Reads a per-project `docs/release.md` contract (the `docs/workflow.md`/`docs/design.md` sibling) naming the medium — versioning scheme, tag format, changelog source, publish targets — and **hardcodes no tool** (semantic-release / release-please / goreleaser / changesets / `npm publish` all selectable via the contract; grep-verifiable). Mechanism is native `gh release create` + `git tag` **in-session** — no CI/GitHub-Actions release bot, no scheduler, no new dependency (upholds no-headless / subscription-auth). loopkit self-ships (dogfood): bump `.claude-plugin/plugin.json` version + sync `marketplace.json` + CHANGELOG + git tag == version + `gh release create`, running `scripts/verify.sh` pre-tag. Foundation impact: **vision** — five → six loop skills + the ship phase / release-contract in scope; **constitution** — new binding release principle (attended, GitHub-native, no-CI/scheduler, contract-driven, tool-agnostic, grep-verifiable) + a `docs/release.md` tech-stack row; **architecture** — new ship component + release flow. Open decision for the spec (backed by `trust-boundary`): publish is the ultimate untrusted-input + privileged-action + external-comms coincidence — fold publish into the milestone-QA gate, add a third gate, or amend the "exactly two gates" rule. **Sequence after `trust-boundary`** (the boundary principle is the hard predecessor of an autonomous publish). Backed by concern "Release/ship phase" (feature: ship-skill) | — | — |
| **ship-inception-bootstrap** *(NEW — 2026-07-08)* — wire `/loopkit:inception` to author `docs/release.md` during the one-time bootstrap (the `docs/workflow.md`/`docs/design.md`-sibling step), ship a **project-agnostic `release.md` template** (placeholders intact, no project values — the quality gate `scripts/verify.sh` already asserts this class), and extend inception `--here`'s readiness sweep to detect a missing/stale release contract (route to the bootstrap step, **not** a second bootstrap inside `/ship`). **Depends on `ship-skill`** (the contract shape must exist first). Mirrors how `design-phase` seeded the contract + the inception wiring. Backed by concern "Release contract bootstrap" (feature: ship-inception-bootstrap) | — | — |

Unlike the P1–P6 chain, some of these features touch **disjoint files** — the
first real parallel frontier, the orchestrator payoff the chain's own narrative
anticipated. But not all: the foundation docs are a shared spine several features
touch, which serialised the early ones — `prior-art-elevation` edited
`docs/vision.md` + `docs/constitution.md` behind `roadmap-iteration` (#10), and
`design-in-the-loop` was seeded depending on `roadmap-iteration` for the same
reason. In practice that spine discharged itself in order: #10 then #11 both
closed before `design-in-the-loop` (#12) was planned, so #12's foundation edits
(the broadened design principle) landed on a clean base and its milestone runs
**independently** (`Depends on milestone: none`). The lesson held — a
shared-spine dependency is real, but on an attended cadence it often resolves by
sequencing rather than blocking.

The 2026-07-02 seed batch (rows `implement-frontier-exits` through
`foundation-dedup`) came out of one `/loopkit:roadmap` pass over a verified
market + codebase analysis; each row's backing prior-art concern is tagged
`(feature: <slug>)` and dated 2026-07-02. Plan-time sequencing note — the batch
has its own shared spines: `skills/implement/SKILL.md` is touched by
`implement-frontier-exits`, `implement-resume-safety`, `ceremony-overhead`, and
`gate-evidence` (serialize in that order); `skills/plan/SKILL.md` by the first
three of those; `README.md` by `repositioning` -> `launch-playbook` (ordered);
the vision/constitution spine by `ceremony-overhead`, `repositioning`, and
`foundation-dedup`. LICENSE/repo-metadata fixes are deliberately NOT seeded —
they are `track:adhoc` fast-lane material, no phase needed.

The 2026-07-08 Fable audit (see `docs/feedback/2026-07-08-fable-audit.md`)
re-scoped nine of these rows and added three — `trust-boundary`,
`model-tier-slots`, `prior-art-threading` — each tagged `[2026-07-08 audit …]`
inline and backed by a prior-art concern. Every re-scope and new seed survived a
three-vote adversarial verification; two candidate findings were dropped as
restatements of the 2026-07-02 batch. Recommended sequencing after the audit:
**`trust-boundary` leads** — it is a hard predecessor of any autonomy change and
fixes the live `/plan` shell-injection defect; `permission-template-hardening`
follows it. `implement-frontier-exits` (a live correctness bug) runs in parallel,
then `implement-resume-safety`, `ceremony-overhead`, `gate-evidence`,
`implement-conflict-recovery` serialize on `skills/implement/SKILL.md` (that is
the shared-file order, NOT a priority — the human points `/plan` at what to build,
per the no-status-marker rule; the maintainer's weighting lives in the audit note). `prior-art-backfill` runs early on the disjoint
`docs/prior-art.md` and MUST precede `repositioning` + `launch-playbook`, which
consume its USP verdicts. `prior-art-threading` and `model-tier-slots` slot after
the implement spine. A durable **autonomy dial is deferred** behind
`trust-boundary` + a ratified constitution amendment (the audit recommends keeping
exactly two hard gates and making today's de-facto QA-skipping auditable via
`gate-evidence` instead). Adjudications resolved 2026-07-08:
`/batch` stays the native fan-out primitive (docs confirm it is not deprecated;
Dynamic Workflows is additive, not a successor) → `repositioning` unblocked; the
autonomy dial is **deferred** (keep exactly two hard gates, `gate-evidence` makes
today's prompt-skipping auditable); the German-diagram exception is **removed**
(English-only); the unscoped `bypassPermissions` is **fixable** via per-skill
`PreToolUse` frontmatter hooks, not a harness limitation. The only medium question
left open is SVG-vs-Mermaid (deferred into `foundation-dedup`/`design-coverage`).

The 2026-07-08 `/ship` seed (`ship-skill`, `ship-inception-bootstrap`) added a
**sixth loop skill**, completing the lifecycle roadmap → plan → implement →
**ship** — the release phase where most SDD tools stop (only BMAD models it). It
is **attended and GitHub-native** by construction: native `gh release create` +
`git tag` in-session, no CI release bot, upholding the no-scheduler /
subscription-auth rules — the differentiator, since every mainstream release tool
(semantic-release, release-please, the Claude-plugin starter) ships as a
GitHub-Actions bot. `ship-skill` sits on the **foundation spine** (vision +
constitution + architecture) and is **sequenced after `trust-boundary`**:
publishing is the sharpest untrusted-input + privileged-action + external-comms
coincidence, so the boundary principle (and its gate question) must land first;
the publish-gate decision itself — fold into milestone-QA, a third gate, or amend
the "exactly two gates" rule — is deferred to `ship-skill`'s `/loopkit:plan`
spec-acceptance. `ship-inception-bootstrap` depends on `ship-skill` (the
`docs/release.md` contract shape must exist before inception can bootstrap it) and
touches `skills/inception/SKILL.md`.

## North star

loopkit becomes the proportional loop — its own development driven by its own
loops, so every change to the tool flows through the same producer/orchestrator
cycle it ships.
